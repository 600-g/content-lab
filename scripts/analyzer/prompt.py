"""Gemini 분석 프롬프트 — v2.6 (사용자 v2 백엔드 교정 지시서 반영).

v2.6 변경 (2026-05-26):
- 본문 7섹션 고정 구조 (callout / 이게 뭔가요? / 따라하기 / 활용 예시 / 💡 아이디어 / 주의사항 / 출처)
- 금지 대제목 7종 강제 (어떻게 작동하나요?, 두근컴퍼니 적용, 원리, 단계 등)
- 원문 보존 최우선 — 코드/프롬프트/JSON 전문 그대로
- callout 필드 분리 (LLM 이 별도 출력 → register 가 Notion callout 블록으로 변환)
- 사용자가 직접 작업한 7개 페이지 패턴이 표준
"""

CATEGORIES = [
    "프롬프트", "자동화", "콘텐츠", "디자인", "개발", "업무", "기타",
]
GRADES = ["S", "A", "B", "C"]
DIFFICULTIES = ["초급", "중급", "고급"]
AI_TOOLS = [
    "Claude", "GPT", "Gemini", "Midjourney", "Leonardo AI", "CapCut", "Canva",
    "Cursor", "Codex", "ComfyUI", "Stable Diffusion", "Ollama", "Claude Code", "도구무관",
]
TARGETS: list[str] = []
TAGS: list[str] = []

# 검증/재시도용 — 금지 대제목 & 허용 대제목
BANNED_HEADINGS = [
    "어떻게 작동하나요", "이렇게 쓰면 효과적", "두근컴퍼니 적용",
    "언제 쓰나", "원리", "단계", "따라 하기 (단계별)",
    "실제 예시", "이렇게 쓰면",
]
ALLOWED_HEADINGS = [
    "이게 뭔가요?", "따라하기", "활용 예시", "💡 아이디어", "주의사항", "출처",
]


ANALYSIS_PROMPT_TEMPLATE = """너는 AI 스킬 백과사전을 만드는 수집 에이전트다.
원본 콘텐츠를 분석하여 노션 DB에 등록할 페이지 본문을 작성한다.

[원본]
- 출처: {url}
- 출처유형: {source_type}
- 제목: {title}

[원본 텍스트]
{text}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚨 최우선 규칙: 원문 보존
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. **코드/프롬프트/명령어/JSON 구조는 절대 요약하거나 축소하지 마라**
   - 원본에 100줄짜리 프롬프트가 있으면 100줄 그대로 코드블록(```)에 넣어라
   - `[[CODE_BLOCK_N]]` 같은 placeholder 치환 절대 금지
   - 이게 이 DB 의 핵심 자산이다

2. **"두근컴퍼니 적용" 같은 2줄 요약으로 본문을 대체하지 마라**
   - 이전에 이걸로 14개 페이지 데이터가 소실됨

3. **간소화가 아닌 최적화** — 길어도 필요하면 유지. 가독성을 높여라
   - 목표: 나무위키/백과사전처럼 처음 보는 사람도 따라할 수 있는 수준

4. **body_md 최소 1500자** — 원본이 2000자 이상이면 본문은 최소 1500자 이상으로 작성
   - 원본의 핵심 정보(예시·코드·단계)는 단 한 줄도 누락 금지
   - 추상화·요약만 가득한 짧은 결과는 즉시 실패로 간주된다
   - 원본이 1000자 미만으로 정말 짧을 때만 본문도 짧게 OK
   - 실 사례: 원본 10,124자 → 본문 662자 만든 적 있음. 이게 절대 다시 일어나선 안 됨

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📐 페이지 본문 구조 (이 순서 고정, 이 대제목만 사용)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. **callout** (별도 필드 — JSON 의 `callout` 키)
   → 이 스킬이 뭔지 1~2문장. 핵심 키워드 **굵게**
   → "이 스킬은 ~하는 스킬입니다" 톤

2. **## 이게 뭔가요?**
   → 배경 설명 2~3문단 + 어떤 AI 도구에서 쓰는지
   → 💰 유료 필요 시 명시 + ✅ 무료 대안 병기

3. **## 따라하기**
   → 번호 매긴 단계별 가이드 (1. 2. 3.)
   → 코드/프롬프트는 ```코드블록``` 으로 원문 그대로
   → 프롬프트 팩이면 ### 소제목으로 각각 구분

4. **## 활용 예시**
   → 실제 시나리오 1~3개
   → "이렇게 입력하면 → 이런 결과" 형태

5. **## 💡 아이디어** (선택)
   → 수익화 가능성, 프로젝트 강화 방안
   → 없으면 이 섹션 자체 생략

6. **## 주의사항** (선택)
   → 흔한 실수, 비용 주의, 한도
   → 없으면 이 섹션 자체 생략

7. **## 출처**
   → [원본 제목](URL)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⛔ 사용 금지 대제목 (절대 쓰지 마라)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

"어떻게 작동하나요?", "이렇게 쓰면 효과적이다", "두근컴퍼니 적용",
"언제 쓰나", "원리", "단계", "따라 하기 (단계별)",
"실제 예시", "이렇게 쓰면"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
서식 규칙
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- callout: 별도 JSON 필드. body_md 안에 넣지 말 것
- 코드블록: 프롬프트/명령어/JSON/코드 전부 ``` 로 감싸라 (인라인 X)
- 굵은글씨: 단계명, 도구명, 핵심 키워드에만 (문장 전체 X)
- 번호 목록: "따라하기" 단계에서만. 나머지는 - (글머리)
- 이모지: callout 외 본문에 남발 금지 (💰✅ 는 비용 표시에만)
- 빈 섹션: 내용 없으면 섹션 자체 삭제 (억지로 채우지 마라)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
등급 기준
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- S-즉시적용: 지금 바로 무료 도구로 실행 가능
- A-참고가치: 가까운 미래에 활용, 약간의 준비 필요
- B-나중에: 직접 관련 없지만 미래에 쓸 수 있음
- C-스킵: 활용 불가 — body_md 비워서 반환 (DB 등록 안 됨)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
두근 환경 (활용 예시 작성 시 참고)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

유료: Claude Max
무료: Gemini, ChatGPT, Gemma4(Ollama 로컬), Bing Image Creator, Leonardo AI, CapCut, Canva
장비: Mac Mini M4, iPhone | 코딩: 초보 (Python 기초, Claude Code CLI)

비용 표시:
- 유료 필요 → "💰 유료 필요: ○○"
- 무료 대안 → "✅ 무료 대안: ○○ 으로 가능" 반드시 병기

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[허용 옵션 — 정확히 일치, 자유형 금지]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

- category 7종 (택1): 프롬프트 / 자동화 / 콘텐츠 / 디자인 / 개발 / 업무 / 기타
- ai_tools 14종 (해당하는 것만 0-5개, **이름 그대로**): Claude / GPT / Gemini / Midjourney / Leonardo AI / CapCut / Canva / Cursor / Codex / ComfyUI / Stable Diffusion / Ollama / Claude Code / 도구무관
- difficulty 3종 (정확히 택1): 초급 / 중급 / 고급

[제목 작성 — skill_title_ko]
- 8-15자, 동사 위주, "이게 뭘 하는지" 즉시 드러나게
- ❌ "AI 비서: 자연어 업무 자동화" (모호한 명사 나열)
- ✅ "이커머스 업무를 자연어로 AI 에 위임" (행동·결과)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
[응답 형식 — JSON 만, 코드블록 없이]
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

{{
  "skill_name": "kebab-case 영문 슬러그",
  "skill_title_ko": "8-15자 동사형 한국어 제목 (이모지 X)",
  "callout": "이 스킬이 뭔지 1~2문장. 핵심 키워드 **굵게** (이게 페이지 맨 위 💡 callout 으로 박힘)",
  "category": "위 7종 중 택1",
  "grade": "S/A/B/C",
  "grade_reason": "등급 사유 1줄",
  "difficulty": "초급/중급/고급",
  "ai_tools": ["위 14종 중 해당 (없으면 [])"],
  "body_md": "## 이게 뭔가요?\\n... (위 7섹션 구조 중 callout/출처 제외한 본문. 코드/프롬프트 전문 보존)"
}}

응답은 반드시 위 JSON 구조만. 다른 텍스트 절대 포함 금지.
body_md 안에 callout 을 다시 넣지 마라 — callout 은 별도 필드다.
"""


# 붙여넣기 입력은 웹페이지 스크랩과 성격이 다르다 — 원본 URL 이 없고, 사용자가 이미
# 필요한 부분만 골라 온 상태다. 그 맥락을 프롬프트에 명시해야 LLM 이 "출처 링크를 넣어라"
# 같은 헛일을 하지 않고, 제목도 본문에서 스스로 뽑는다.
PASTE_CONTEXT = """
[입력 방식] 사용자가 본문을 **직접 붙여넣었다** (웹 스크랩 아님).
- 원본 URL 이 없으므로 출처 링크를 지어내지 마라. 출처 섹션은 "직접 입력한 텍스트" 로 둔다.
- 위 '제목' 은 본문 첫 줄에서 기계적으로 뽑은 값이라 부정확할 수 있다.
  본문 내용을 근거로 skill_title_ko 를 새로 지어라.
- 사용자가 이미 필요한 부분만 골라 넣은 상태다. 분량이 웹페이지보다 짧아도
  그 자체로 감점하지 말고, 담긴 내용의 실용성으로만 등급을 매겨라.
"""


def build_prompt(scrape_dict: dict) -> str:
    """ScrapeResult.to_dict() → 분석 프롬프트."""
    meta = scrape_dict.get("meta") or {}
    is_paste = scrape_dict.get("source_type") == "text" or meta.get("input_kind") == "paste"
    url = scrape_dict["url"]
    # paste://<hash> 는 우리 내부 식별자다. 프롬프트에 그대로 노출하면 LLM 이 그걸
    # 진짜 URL 로 착각해 본문에 [제목](paste://...) 죽은 링크를 박는다 (실측 확인).
    if is_paste and str(url).startswith("paste://"):
        url = "(직접 입력한 텍스트 — 원본 링크 없음)"
    base = ANALYSIS_PROMPT_TEMPLATE.format(
        url=url,
        source_type=scrape_dict["source_type"],
        title=scrape_dict.get("title", "") or "(제목 없음)",
        text=(scrape_dict.get("text") or "")[:150000],
    )
    return base + PASTE_CONTEXT if is_paste else base
